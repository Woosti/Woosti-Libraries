{
  "rules": {
    "ban": [
      true,
      {
        "name": ["shortid"],
        "message": "use `utils/uid`'s `uid` instead."
      },
      { "name": ["uuid"], "message": "use `utils/uid`'s `uid` instead." }
    ]
  }
}
